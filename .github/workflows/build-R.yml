name: build-R
concurrency:
  group: build-R-${{ github.ref }}
  cancel-in-progress: true
on: 
#   pull_request: 
#     paths-ignore:
#       - '**/*.md'
  push:
    paths-ignore:
      - '**/*.md'
  workflow_dispatch:
jobs:
  build-R:
    name: Run R tests
    runs-on: "ubuntu-latest"
    defaults:
      run:
        shell: bash -el {0}
    steps:
      - uses: actions/checkout@v2
      - uses: conda-incubator/setup-miniconda@v2
        with:
          miniconda-version: "latest"
          use-only-tar-bz2: true  # IMPORTANT: This needs to be set for caching to work properly!
          auto-update-conda: true
          auto-activate-base: true
          channels: conda-forge
      - run: conda install -c conda-forge mamba
      - run: mamba env create --file environment.yml -n sfds # Create the conda environment named sfds.
      - run: conda info --envs
      - run: mamba activate sfds # Activate the environment we created.
#       - run: conda env update --file environment.yml -n sfds # Update the depencies of the environment from environment.yml 
      - run: conda list
          
